# Wolfie MCP Simulation App

Phase 1 keeps the simulated Robinhood-compatible MCP implementation in `wolfie_core/mcp/robinhood_sim.py`.

This directory is reserved for future MCP server process packaging. It must not connect to Robinhood or any real broker in `SIMULATED_LIVE_MCP_LOCAL` mode.

